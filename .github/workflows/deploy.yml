name: Deploy

on:
  push:
    branches: [develop, main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - run: npm ci

      - name: Install functions dependencies
        run: npm ci
        working-directory: functions

      - name: Build
        run: npm run build

      - name: Deploy staging
        if: github.ref == 'refs/heads/develop'
        run: |
          npx firebase-tools functions:config:set football.api_key="${{ secrets.VITE_API_KEY_STAGING }}" --project sportstakes-staging --token ${{ secrets.FIREBASE_TOKEN }}
          npx firebase-tools deploy --only hosting,functions --project sportstakes-staging --token ${{ secrets.FIREBASE_TOKEN }}

      - name: Deploy production
        if: github.ref == 'refs/heads/main'
        run: |
          npx firebase-tools functions:config:set football.api_key="${{ secrets.VITE_API_KEY_PROD }}" --project sportstakes-prod --token ${{ secrets.FIREBASE_TOKEN }}
          npx firebase-tools deploy --only hosting,functions --project sportstakes-prod --token ${{ secrets.FIREBASE_TOKEN }}
